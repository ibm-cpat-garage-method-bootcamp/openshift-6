const item = require('./list.jsx');

describe('list', ()=> {
    test('canary', ()=> {
        expect(true).toEqual(true);
    });

    test('I can view recently used items', () => {
        
    });
});